fastapi
uvicorn[standard]
pydantic
requests
psutil
# UploadFile/Form endpoints (TTS upload) fail at import time without this.
python-multipart
# tts-say: голос Google, офлайн-фоллбек — системный espeak-ng.
gTTS
# YouTube для mpv. Протухает за месяцы — kiosk-pkg-update.timer обновляет
# его еженедельно; апт-версия на год старше и потому не используется.
yt-dlp
# remote (пульт HAOBO + голосовой ассистент): Flask-сервер и evdev-слушатель.
flask
evdev
