# HANDOFF — состояние работ

Читать первым, когда Олег говорит «продолжаем». Долгоживущие знания о проекте —
в [CLAUDE.md](CLAUDE.md); здесь только то, что верно на момент передачи и
устаревает.

**Последнее обновление:** 2026-08-06, ~20:20 CEST, сессия на самом pi4.

**Свежее (ревизия всего кода + новые фичи):**

- `main.py` прошёл полную ревизию: вычищены дубли импортов и мёртвый код,
  починены **часы** (запускали несуществующий `clock_tk.py`; ещё две гнилые
  вещи — pgrep ловил сам себя, PyQt5 не был установлен), исправлен битый JS
  температуры у вебки. Зашитые в webcam доступы — **не утечка**: камера чужая
  и публичная, владельцы сами выложили доступ; дефолты оставлены (CLAUDE.md
  §6.16).
- Новые эндпоинты: `health`, `screenshot`, `logs`, `restore` + **веб-пульт**
  `GET /surface/remote` (страница для телефона). Список маршрутов в `GET /`
  теперь автогенерируется.
- Установщик ставит `scrot` и `python3-pyqt5`, копирует `remote.html`.
- Всё проверено живьём на pi4: смоук-тест всех read-only — 200, часы
  вкл/выкл, скриншот отдаётся, пульт отрендерен, браузер жив.
- Не проверялись живьём (нужны внешние ресурсы/уши): `webcam`, `immich/album`,
  `tts-play-upload`, `play`, `toggle-fill` с MPV.
- Олег поймал: `display-off` — и экран сам зажигался через полминуты. Это был
  `kiosk-output.timer`. Исправлено флагом `/var/lib/kiosk/display_off`,
  проверено с двумя тиками сторожа (CLAUDE.md §6.17). AirPlay-цепочку Олег
  прогнал с живого iPhone в 20:27 — браузер ушёл, после отключения вернулся
  сам за 4 секунды.
- Олег поймал второе (2026-08-07 утром): AirPlay к **погашенному** экрану —
  зеркало рисовалось на тёмной панели. Теперь `airplay-watch` при старте
  сессии будит экран (`display-on`), потом убирает браузер. Прогнана вся
  матрица стыков: выкл+AirPlay (просыпается), выкл+`restore`/`chrome-tabs`
  (просыпается, флаг снят), выкл+тики сторожа (спит), конец сессии (вкладки
  через 4с). Имитация сессии без телефона — `nc` на порт uxplay, CLAUDE.md
  §6.17.
До этого работа шла с Mac по SSH — Wi-Fi рвал сессии посреди операций,
и половина времени уходила на циклы с ретраями.

---

## Где ты сейчас

Ты запущен **на pi4** — это тестовый стенд, он же будущий киоск. Значит:

- `~/kiosk` — **одна папка на всё**: это и git-репозиторий, и установка.
  Исходники (`install.sh`, `scripts/`, `systemd/`) лежат в корне, а то, что
  реально запускает systemd, — в `~/kiosk/app` (`main.py`, `.venv`).
  Отдельной `~/pi-kiosk` больше нет, Олег просил не плодить папки;
- `/etc/kiosk/kiosk.env`, `/var/log/kiosk.log`, `/var/lib/kiosk/` — здесь же;
- `sudo` без пароля;
- SSH до других машин отсюда **не настроен** — раньше он был с Mac. Если
  понадобится 3B (192.168.1.133), NAS (192.168.1.95) или роутер (192.168.1.1),
  спросить у Олега пароли и поставить `sshpass`, либо попросить его выполнить
  команду через `!` в своей сессии.

`git push` отсюда не проверялся. `gh` установлен, но не авторизован — если
понадобится пушить, сначала `gh auth login`.

---

## Что сделано и проверено

Работает на pi4, подтверждено измерением, а не предположением:

- установка из репозитория одной командой, `INSTALL_EXIT=0`, пять прогонов;
- `xinit`, `surface-api`, `net-watchdog.timer`, `kiosk-restore`,
  `kiosk-output.timer`, `airplay-video`, `airplay-audio`, `airplay-watch.timer`
  — все `active`;
- API на `:7000` отвечает `200`, в том числе **из сети** (после починки ufw);
- все read-only эндпоинты отвечают `200`;
- `kiosk-restore` возвращает три вкладки, `HTTP 200` с первой попытки;
- AirPlay публикуется: `_airplay._tcp → pi4` и `_raop._tcp → pi4 Audio`,
  Олег подтвердил, что видит устройство с iPhone;
- `airplay-watch` ловит начало и конец сессии (видно в `/var/log/kiosk.log`);
- гашение панели реально снимает подсветку (`bl_power` 0 → 4);
- **возврат вкладок после AirPlay** — проверен сквозным прогоном (см. ниже,
  там был молчаливый баг, теперь исправлен: коммит `a6ae7a1`);
- эндпоинты `display-off`/`display-on`/`display-state` — работают и не врут
  (`bl_power` 0 → 4 → 0 совпадает с ответами API);
- `kill-all` — фактически проверен: его дёргает `airplay-watch` при старте
  сессии, Chromium реально исчезал;
- `custom-command` (POST) и `custom-command-get` — отвечают, команды выполняются;
- `toggle-fill` без запущенного MPV корректно отдаёт `404 MPV не запущен`.

**Найденный и исправленный баг:** `airplay-watch.sh` запускал `kiosk-restore.sh`
в фоне через `&`, но сам работает как `Type=oneshot` — systemd зачищал cgroup
сразу после выхода основного скрипта и убивал restore до того, как тот успевал
хоть что-то сделать. Ни строки в логе, ни записи в журнале — классический
молчаливый отказ. Вкладки после реальной сессии в 19:31 вернул не он, а ручной
вызов API с Mac (в журнале виден запрос с 192.168.1.50). Теперь скрипт делает
`systemctl restart --no-block kiosk-restore.service` (именно `restart`: у юнита
`RemainAfterExit=yes`, обычный `start` был бы no-op). Проверено: подложил
`1` в `/var/lib/kiosk/airplay_client`, таймер прошёл ветку «сессия кончилась»,
restore отработал, HTTP 200, вкладки на экране (скриншот).

На **3B** (192.168.1.133): comitup отключён, power save выключен, watchdog и
восстановление экрана работают — но это **ручная установка на старых путях**,
не из репозитория. См. CLAUDE.md §2.

На **NAS**: перегрузку вызывал контейнер `immich-kiosk`, Олег поправил его
конфиг, load average упал 14.85 → 1.77, активные запросы postgres 8 → 0.

---

## Что НЕ проверено — начинать отсюда

### 1. AirPlay: видно ли трансляцию (главное)

**ПРОВЕРЕНО ОЛЕГОМ: работает отлично.** Полный цикл с iPhone: браузер уходит,
трансляция видна, после отключения вкладки возвращаются.

«Чёрный экран с мышкой», который Олег видел после AirPlay, оказался не про
AirPlay: браузер убил рестарт `surface-api` при повторном прогоне установщика
(Chromium жил в cgroup сервиса). Исправлено `KillMode=process`, см. CLAUDE.md
§6.14 — и заодно §6.15 про то, почему restore нельзя запускать из API напрямую.

**Добавлены эндпоинты управления AirPlay** (`airplay-state`/`-on`/`-off`/
`-kick`), все проверены живьём; для них расширен `config/sudoers-kiosk`.

### 2. Переключение на HDMI (отложено — кабеля пока нет)

Логика написана и отрабатывает вхолостую (`active_output=DSI-1`), но кабель к
pi4 ни разу не подключали. Попросить Олега воткнуть HDMI и через 30 секунд:

```bash
cat /var/lib/kiosk/active_output      # ждём HDMI-1
cat /sys/class/backlight/*/bl_power   # ждём 4 — панель погашена
DISPLAY=:0 xrandr | grep " connected"
```

Проверить заодно, что `surface-api` перезапустился и `display-state` не врёт.

### 3. Расписание гашения

Код есть, но ни разу не включалось (`DISPLAY_OFF_AT`/`DISPLAY_ON_AT` пусты).
Олег изначально спрашивал не про расписание, а про HDMI — уточнить, нужно ли
оно вообще.

### 4. Эндпоинты, меняющие состояние — остатки

Проверены: `kill-all`, `display-off`/`display-on`/`display-state`,
`custom-command` (обе формы), `toggle-fill` (ветка без MPV). Остались:

- `reboot` — сознательно не дёргался: сессия Claude работает на этой же машине,
  перезагрузка убьёт её саму. Проверять в конце какой-нибудь сессии.
- `webcam`, `immich/album`, `tts-play-upload` — нужны параметры (URL камеры,
  ключ Immich, аудиофайл) и/или проверка звука ушами.
- `toggle-fill` с запущенным MPV.

---

## Открытые вопросы к Олегу

- **3B не трогаем** — Олег решил: забываем про перенос на репозиторий,
  пусть живёт на ручной установке.
- **Wi-Fi.** Обе его машины теряют связь, пока пять других устройств висят в
  сети по пять суток. Он просил роутер не трогать. Предлагавшиеся изменения:
  `psk-mixed → psk2` (убрать TKIP) и `random_bssid → 0`. Кратковременно
  отключит Wi-Fi всем в доме.
- Что показывать на pi4, когда он станет боевым — сейчас у него тот же
  `COMBO_URL`, что и у 3B, то есть оба показывают одно и то же.

---

## Как здесь принято работать

- Проверять фактами: `curl`, `systemctl`, логи, скриншот экрана. Сегодня три
  диагноза оказались неверными, и дважды виноват был **инструмент проверки**,
  а не система (CLAUDE.md §6.13).
- Скриншот киоска — самый полезный приём: `scrot -o /tmp/s.png` под
  `DISPLAY=:0`, затем посмотреть глазами.
- Молчаливые отказы здесь норма: пустой юнит-файл читается как `masked`,
  скрипт без прав пишет мимо и возвращает `exit 0`, uxplay буферизует вывод.
  Проверять результат, а не код возврата.
- Репозиторий публичный — паролей и ключей в файлы не класть.
