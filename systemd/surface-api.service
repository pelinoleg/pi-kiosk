[Unit]
Description=Surface Control API
After=network.target xinit.service
Wants=xinit.service

[Service]
Type=simple
User=__USER__
WorkingDirectory=__INSTALL_DIR__
Environment="INSTALL_DIR=__INSTALL_DIR__"
Environment="PATH=__INSTALL_DIR__/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
Environment="DISPLAY=:0"
Environment="XAUTHORITY=__HOME__/.Xauthority"
Environment="XDG_RUNTIME_DIR=/run/user/__UID__"
EnvironmentFile=-/etc/kiosk/kiosk.env
ExecStart=__INSTALL_DIR__/start-surface-api.sh
Restart=always
RestartSec=10
# The API launches Chromium as a child, so with the default control-group
# kill mode every service restart (installer re-run, output switch, crash
# recovery) silently takes the browser down with it and leaves a black
# screen. Kill only the API itself; chrome-tabs reaps stale browsers anyway.
KillMode=process

[Install]
WantedBy=multi-user.target
